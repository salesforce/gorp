/**
 * Package that contains internal representation of the
 * extraction definitions, both in "uncooked" (or raw) form
 * (produced from tokenization) and "cooked" (processed) form
 * that requires resolution and expansion of named references,
 * as well as escaping and/or massaging for literal patterns
 * and text segments.
 */
package com.salesforce.gorp.model;
